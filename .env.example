# Proxmox Configuration
PROXMOX_HOST=https://192.168.1.100:8006
PROXMOX_USER=root@pam
PROXMOX_TOKEN_NAME=dashboard
PROXMOX_TOKEN_VALUE=your-token-here
PROXMOX_VERIFY_SSL=false

# Docker Configuration
# DOCKER_GID: Host Docker group ID so the container can read the socket.
# Find it with: stat -c '%g' /var/run/docker.sock
DOCKER_GID=996
# DOCKER_URL: Base URL for clickable container links in the dashboard (optional)
DOCKER_URL=http://192.168.1.100

# Radarr Configuration
RADARR_URL=http://192.168.1.100:7878
RADARR_API_KEY=your-radarr-api-key

# Sonarr Configuration
SONARR_URL=http://192.168.1.100:8989
SONARR_API_KEY=your-sonarr-api-key

# Lidarr Configuration (optional)
LIDARR_URL=http://192.168.1.100:8686
LIDARR_API_KEY=your-lidarr-api-key

# Jellyfin Configuration (optional — for streaming sessions)
JELLYFIN_URL=http://192.168.1.100:8096
JELLYFIN_API_KEY=your-jellyfin-api-key

# Plex Configuration (optional — for streaming sessions)
# Use either Plex direct OR Tautulli, not both (to avoid duplicate sessions)
PLEX_URL=http://192.168.1.100:32400
PLEX_TOKEN=your-plex-token

# Tautulli Configuration (optional — richer Plex streaming info)
TAUTULLI_URL=http://192.168.1.100:8181
TAUTULLI_API_KEY=your-tautulli-api-key

# Claude AI Chat (optional)
# Get an API key at https://console.anthropic.com/
CLAUDE_API_KEY=your-anthropic-api-key
# Model ID — see https://docs.anthropic.com/en/docs/about-claude/models for current options.
# Defaults to the latest Sonnet model if unset.
CLAUDE_MODEL=claude-sonnet-4-5

# Uptime Kuma (optional — monitoring dashboard)
UPTIME_KUMA_URL=http://192.168.1.100:3001
# Optional: enable per-monitor status in HomePulse by providing an API
# key issued from Uptime Kuma's Settings > API Keys. When set, HomePulse
# pulls /metrics (Prometheus format) and shows each monitor's up/down
# status, response time, and cert days. Without this token, the widget
# falls back to a single online/offline card.
UPTIME_KUMA_METRICS_TOKEN=

# Infrastructure Monitoring (optional — SSL cert tracking via NPM)
NPM_URL=http://192.168.1.100:81
NPM_API_TOKEN=your-npm-jwt-token

# NAS / external storage mounts (optional).
# Comma-separated list of filesystem paths visible INSIDE the container
# that should appear in the Infrastructure widget. Mount them into the
# container via docker-compose volumes first (read-only is fine).
# Example: NAS_MOUNTS=/mnt/nas,/mnt/backup
NAS_MOUNTS=

# Notifications (optional — Telegram alerts)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-telegram-chat-id

# Dashboard Settings
REFRESH_INTERVAL=30

# When true, the dashboard read endpoints require either a JWT bearer
# token or an X-API-Key header. Defaults to false for backward
# compatibility (dashboard is open). Create API keys via the admin
# Settings panel > API Keys tab.
DASHBOARD_REQUIRE_AUTH=false

# Comma-separated list of CORS origins allowed to make credentialed
# requests to the API when DASHBOARD_REQUIRE_AUTH=true. Ignored when
# auth is off (CORS stays wildcard for LAN convenience). Empty list
# + auth on = same-origin only.
# Example: ALLOWED_ORIGINS=https://home.example.com,http://192.168.1.5:8450
ALLOWED_ORIGINS=

# Optional: credentials for private registries used in container-update
# checks. JSON object keyed by registry host. Only needed for images on
# non-public registries (e.g. ghcr.io private repos). Public images
# (Docker Hub, lscr.io, ghcr.io public) work with no auth.
# Example: REGISTRY_AUTH_JSON={"ghcr.io":{"username":"you","password":"ghp_..."}}
REGISTRY_AUTH_JSON=

# Authentication (optional — auto-generated if not set, but sessions reset on restart)
JWT_SECRET=your-random-secret-here
